# 管理后台 - 主 Agent 协调规范

本文件用于约束主 Agent 与各子 Agent 在本仓库中的协作边界。所有修改都必须遵守目录归属、质量门禁和编码规范。

## 1. 任务分发原则

- 涉及 Frontend 角色的代码改动，必须交给 Frontend 子 Agent，主 Agent 不得直接修改 `/frontend` 下代码。
- 涉及 Backend 角色的代码改动，必须交给 Backend 子 Agent，主 Agent 不得直接修改 `/backend` 下代码。
- Planner 只负责需求拆解、数据库设计、API 契约和任务规划，不直接修改业务代码。
- Reviewer 只负责代码质量、契约一致性、测试覆盖和风险审查，默认不直接修改业务代码。
- 主 Agent 负责流程协调、根目录配置、集成验证、文档整理和不归属于子 Agent 的问题修复。
- 违反目录边界或角色边界视为流程违规。

## 2. 质量门禁与自动修复循环

### 2.1 循环流程

```text
Planner 输出规划
  → Frontend 与 Backend 并行开发
  → Reviewer 进行质量审查
    → 如果存在 P1 或 P2 问题
      → 对应子 Agent 修复
      → Reviewer 重新审查
      → 重复直到 P1 清零、P2 不超过 2 个，或达到最大循环次数
    → 通过后进入集成验证
  → 主 Agent 执行集成验证
    → 启动前后端服务、联调测试、检查核心流程
    → 如果发现集成问题，回退给对应子 Agent 修复
    → 通过后结束
```

### 2.2 问题等级

- **P1 阻塞问题**：必须修复，阻塞发布。
- **P2 重要问题**：建议修复；同一模块循环 3 次仍无法解决时，可降级为待办项，但必须记录原因。
- **P3 一般问题**：记录到待办清单，不阻塞当前循环。

### 2.3 修复规则

- Frontend 只修改 `/frontend`。
- Backend 只修改 `/backend`。
- 主 Agent 只修改根目录配置、文档、集成脚本，以及不归属具体子 Agent 的文件。
- 子 Agent 修复完成后必须说明修改文件列表，方便 Reviewer 增量审查。

### 2.4 集成验证

代码审查通过后，由主 Agent 执行集成验证，至少检查以下内容：

- 前后端启动是否正常，包括端口、数据库连接和编译。
- 前后端联调是否正常，包括 API 代理、CORS、路由和认证。
- 核心流程是否可用，包括登录、菜单加载、列表查询、数据提交。
- 部署配置是否正确，包括 `vite.config.ts` 代理、后端配置文件、Docker Compose 配置。
- 运行环境是否兼容，包括数据库版本、驱动、Go 版本和 Node 版本。

集成验证发现的问题按同等级别回退到修复循环中处理。

### 2.5 人工介入条件

- 同一问题反复出现 3 次仍未解决，标记为需人工介入，并跳出自动循环。
- 跨模块或跨目录的架构问题，由主 Agent 汇总后等待人工决策。
- 集成验证发现的环境或配置问题，子 Agent 无法独立解决时，由主 Agent 处理或请求人工确认。

## 3. 编码规范

### 3.1 中文字符编码

- 所有源码、配置和文档文件必须使用 UTF-8 编码且不含 BOM。
- 所有中文文本必须保存为真实中文字符，禁止使用 Unicode 转义序列表示中文。
- 写入文件时，必须确保非 ASCII 字符原样保持，不被自动转义。

### 3.2 界面文案

模板和组件中的中文文本，例如 label、placeholder、message，必须是可读中文字符。

正确示例：

```vue
<el-form-item label="部门名称" prop="name">
  <el-input placeholder="请输入部门名称" />
</el-form-item>
```

错误示例：

```vue
<el-form-item label="Unicode 转义后的部门名称" prop="name">
  <el-input placeholder="Unicode 转义后的请输入部门名称" />
</el-form-item>
```

### 3.3 Reviewer 审查项

Reviewer 必须检查源码、配置和文档中是否存在以下问题：

- 中文文本被写成转义形式。
- 文件包含 BOM。
- Frontend 或 Backend 修改了自己职责范围外的目录。
- API 契约、类型定义、路由路径、权限编码不一致。
- 测试、类型检查或构建命令未通过。

发现中文转义或 BOM 问题时，应标记为 P1。